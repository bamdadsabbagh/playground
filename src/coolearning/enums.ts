export enum Settings {
    Parameter = 0,
    Control = 1,
    Type = 2,
    Action = 3,
}

export enum Actions {
    Learn = 'learn',
    Unlearn = 'unlearn',
}

export enum State {
    IsLearning = 'isLearning',
    LearningParameter = 'learningParameter'
}